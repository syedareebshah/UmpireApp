export interface AppState {
  currentRouteName: string;
  isDark: boolean;
}
export interface UserState {
  name: string;
  loggedIn: boolean;
  oversCount: number;
  overHistory: any;
  scoreCount: number;
  wicketCount: number;
}
